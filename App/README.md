# Application shell

This directory will contain the signed SwiftUI/AppKit menu-bar application and its Xcode project membership. It depends on the local `LocalASRKit` package and acts as the composition root.

Feature folders contain presentation code only. Domain workflow and infrastructure implementations belong in their package modules.

