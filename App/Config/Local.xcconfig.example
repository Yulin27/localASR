// Copy this file to Local.xcconfig in the same folder. Local.xcconfig is ignored by git.
//
// Find your team ID in Xcode → Settings → Accounts → (your Apple ID) → Personal Team, or in the
// "OU" field of: security find-certificate -c "Apple Development" -p | openssl x509 -noout -subject

DEVELOPMENT_TEAM = YOURTEAMID
