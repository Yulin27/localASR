# Model assets

Downloaded weights and compiled models are runtime artifacts and must not be committed here. `manifest.json` is the versioned inventory; it is intentionally empty until model benchmarks select the v0.1 runtime.

