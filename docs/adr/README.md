# Architecture Decision Records

ADRs record decisions that affect module boundaries, dependencies, deployment, privacy, or major runtime behavior.

Use sequential filenames such as `0002-asr-runtime.md`. A record should include status, context, decision, consequences, and alternatives. Do not rewrite an accepted decision invisibly; add a new ADR that supersedes it.

